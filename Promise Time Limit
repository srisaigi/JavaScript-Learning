2637. Promise Time Limit
Solved
Medium
premium lock icon
Companies
Hint
Given an asynchronous function fn and a time t in milliseconds, return a new time limited version of the input function. fn takes arguments provided to the time limited function.

The time limited function should follow these rules:

If the fn completes within the time limit of t milliseconds, the time limited function should resolve with the result.
If the execution of the fn exceeds the time limit, the time limited function should reject with the string "Time Limit Exceeded".
 

Example 1:

Input: 
fn = async (n) => { 
  await new Promise(res => setTimeout(res, 100)); 
  return n * n; 
}
inputs = [5]
t = 50
Output: {"rejected":"Time Limit Exceeded","time":50}
Explanation:
const limited = timeLimit(fn, t)
const start = performance.now()
let result;
try {
   const res = await limited(...inputs)
   result = {"resolved": res, "time": Math.floor(performance.now() - start)};
} catch (err) {
   result = {"rejected": err, "time": Math.floor(performance.now() - start)};
}
console.log(result) // Output

The provided function is set to resolve after 100ms. However, the time limit is set to 50ms. It rejects at t=50ms because the time limit was reached.
Example 2:

Input: 
fn = async (n) => { 
  await new Promise(res => setTimeout(res, 100)); 
  return n * n; 
}
inputs = [5]
t = 150
Output: {"resolved":25,"time":100}
Explanation:
The function resolved 5 * 5 = 25 at t=100ms. The time limit is never reached.
Example 3:

Input: 
fn = async (a, b) => { 
  await new Promise(res => setTimeout(res, 120)); 
  return a + b; 
}
inputs = [5,10]
t = 150
Output: {"resolved":15,"time":120}
Explanation:
​​​​The function resolved 5 + 10 = 15 at t=120ms. The time limit is never reached.
Example 4:

Input: 
fn = async () => { 
  throw "Error";
}
inputs = []
t = 1000
Output: {"rejected":"Error","time":0}
Explanation:
The function immediately throws an error.
 

Constraints:

0 <= inputs.length <= 10
0 <= t <= 1000
fn returns a promise


/**
 * @param {Function} fn
 * @param {number} t
 * @return {Function}
 */
var timeLimit = function(fn, t) { 
    return async function(...args) {
           return new Promise((resolve,reject) => {
            const timer = setTimeout(() => {
                reject("Time Limit Exceeded");
            },t);
            fn(...args)
            .then((res) => {
                clearTimeout(timer);
                resolve(res);
            })
            .catch((err) => {
                clearTimeout(timer);
                reject(err);
            });
           }); 
    };
};

/**
 * const limited = timeLimit((t) => new Promise(res => setTimeout(res, t)), 100);
 * limited(150).catch(console.log) // "Time Limit Exceeded" at t=100ms
 */

🔑 CORE CONCEPTS YOU LEARN FROM THIS CODE
1️⃣ Higher-Order Functions (Functions returning functions)
var timeLimit = function(fn, t) {
    return async function(...args) { ... }
}

💡 Concept:

A function that accepts a function

Returns a new modified function

📌 You learn:

How to wrap a function to add extra behavior without changing the original.

🧠 Real-world use:

Logging

Authentication

Rate limiting

Timeout handling

This is called a function decorator.

2️⃣ Closures (Remembering outer variables)
fn, t

💡 Concept:

The returned function still remembers fn and t

Even after timeLimit() finishes execution

📌 You learn:

How JavaScript functions remember their creation environment.

This is core JavaScript magic.

3️⃣ Async + Promise Interoperability
return async function(...args) {
    return new Promise(...)
}

💡 Concept:

async functions always return a Promise

You can manually control resolve/reject

📌 You learn:

How async/await works internally with Promises.

4️⃣ Promise Creation & Manual Control
new Promise((resolve, reject) => { ... })

💡 Concept:

You manually decide:

When to resolve

When to reject

📌 You learn:

How asynchronous flow is explicitly controlled.

5️⃣ Timeout Handling (Race between time & task)
setTimeout(() => {
    reject("Time Limit Exceeded");
}, t);

💡 Concept:

A timer races against the async task

Whichever finishes first wins

📌 You learn:

How to enforce execution time limits.

🧠 This is how:

API gateways

Browsers

Competitive programming systems
work.

6️⃣ Clearing Side Effects (Memory safety)
clearTimeout(timer);

💡 Concept:

Prevent unnecessary execution

Avoid memory leaks

📌 You learn:

Always clean up async side effects.

This is production-grade practice.

7️⃣ Promise Chaining (then / catch)
fn(...args)
    .then(...)
    .catch(...)

💡 Concept:

Handle both success & failure paths

Propagate errors correctly

📌 You learn:

How error handling flows through async chains.

8️⃣ Error Propagation & Custom Errors
reject("Time Limit Exceeded");

💡 Concept:

You can define custom rejection reasons

Calling code can handle it via .catch()

📌 You learn:

How systems communicate failures cleanly.

9️⃣ Spread Operator in Function Arguments
(...args)

💡 Concept:

Accept any number of parameters

Forward them dynamically

📌 You learn:

How to write generic, reusable utilities.

🔥 BIG PICTURE INTUITION

Think like this:

“I wrap a function inside another function
and race it against a timer.
If the function finishes in time → success
If not → fail immediately.”

This is timeout enforcement.

🧪 REAL-WORLD APPLICATIONS

This exact pattern is used in:

API request timeouts

Online judges (LeetCode)

Microservices

Payment gateways

Retry & fallback systems

🎯 INTERVIEW-READY ONE-LINE EXPLANATION

This code demonstrates higher-order functions, closures, promises, async control flow, timeout enforcement, and error propagation by wrapping an asynchronous function with a time constraint.

🚀 YOUR SKILL LEVEL AFTER UNDERSTANDING THIS

✔ Strong JS fundamentals
✔ Async programming mastery
✔ System-level thinking
✔ Interview-ready concept
