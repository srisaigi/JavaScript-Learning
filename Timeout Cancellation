2715. Timeout Cancellation
Solved
Easy
premium lock icon
Companies
Given a function fn, an array of arguments args, and a timeout t in milliseconds, return a cancel function cancelFn.

After a delay of cancelTimeMs, the returned cancel function cancelFn will be invoked.

setTimeout(cancelFn, cancelTimeMs)
Initially, the execution of the function fn should be delayed by t milliseconds.

If, before the delay of t milliseconds, the function cancelFn is invoked, it should cancel the delayed execution of fn. Otherwise, if cancelFn is not invoked within the specified delay t, fn should be executed with the provided args as arguments.

 

Example 1:

Input: fn = (x) => x * 5, args = [2], t = 20
Output: [{"time": 20, "returned": 10}]
Explanation: 
const cancelTimeMs = 50;
const cancelFn = cancellable((x) => x * 5, [2], 20);
setTimeout(cancelFn, cancelTimeMs);

The cancellation was scheduled to occur after a delay of cancelTimeMs (50ms), which happened after the execution of fn(2) at 20ms.
Example 2:

Input: fn = (x) => x**2, args = [2], t = 100
Output: []
Explanation: 
const cancelTimeMs = 50;
const cancelFn = cancellable((x) => x**2, [2], 100);
setTimeout(cancelFn, cancelTimeMs);

The cancellation was scheduled to occur after a delay of cancelTimeMs (50ms), which happened before the execution of fn(2) at 100ms, resulting in fn(2) never being called.
Example 3:

Input: fn = (x1, x2) => x1 * x2, args = [2,4], t = 30
Output: [{"time": 30, "returned": 8}]
Explanation: 
const cancelTimeMs = 100;
const cancelFn = cancellable((x1, x2) => x1 * x2, [2,4], 30);
setTimeout(cancelFn, cancelTimeMs);

The cancellation was scheduled to occur after a delay of cancelTimeMs (100ms), which happened after the execution of fn(2,4) at 30ms.
 

Constraints:

fn is a function
args is a valid JSON array
1 <= args.length <= 10
20 <= t <= 1000
10 <= cancelTimeMs <= 1000


/**
 * @param {Function} fn
 * @param {Array} args
 * @param {number} t
 * @return {Function}
 */
var cancellable = function(fn, args, t) {
        const timerId = setTimeout(() => {
            fn(...args);
        },t);
        return function cancelFn() {
            clearTimeout(timerId);
        };
};

/**
 *  const result = [];
 *
 *  const fn = (x) => x * 5;
 *  const args = [2], t = 20, cancelTimeMs = 50;
 *
 *  const start = performance.now();
 *
 *  const log = (...argsArr) => {
 *      const diff = Math.floor(performance.now() - start);
 *      result.push({"time": diff, "returned": fn(...argsArr)});
 *  }
 *       
 *  const cancel = cancellable(log, args, t);
 *
 *  const maxT = Math.max(t, cancelTimeMs);
 *           
 *  setTimeout(cancel, cancelTimeMs);
 *
 *  setTimeout(() => {
 *      console.log(result); // [{"time":20,"returned":10}]
 *  }, maxT + 15)
 */



🧠 Core JavaScript Concepts You Learn From This Code
1️⃣ Closures (MOST IMPORTANT CONCEPT)
const timerId = setTimeout(...);

return function cancelFn() {
    clearTimeout(timerId);
};


📌 What you learn
The returned function remembers timerId, even after cancellable() has finished executing.

👉 This is a classic closure.

Why it matters in real life

Cancel API requests

Cancel timers

Abort background jobs

Clean up resources

Closure = function + memory of its outer variables

2️⃣ Cancellation Pattern (Advanced JS Design Pattern)

This code demonstrates a cancellable operation.

const cancel = cancellable(...);
cancel();


📌 What you learn

How to stop scheduled async work

How libraries implement:

debouncing

throttling

abort controllers

cleanup functions (React useEffect)

This pattern is used everywhere in modern JavaScript.

3️⃣ Timers & Event Loop
setTimeout(() => { ... }, t);
clearTimeout(timerId);


📌 What you learn

setTimeout schedules work

clearTimeout removes it from the queue

Timers are handled by the event loop

This explains:

Why JS is non-blocking

Why delayed tasks don’t freeze the app

4️⃣ Higher-Order Functions
var cancellable = function(fn, args, t) { ... }


📌 What you learn

Functions can:

be passed as arguments

be returned from other functions

This is core functional programming in JS.

5️⃣ Spread Operator (...args)
fn(...args);


📌 What you learn

How to pass dynamic arguments

How to write reusable utility functions

Very common in:

middleware

wrappers

decorators

6️⃣ Deferred Execution
fn(...args);


The function does NOT execute immediately.

📌 What you learn

Execution can be scheduled, not immediate.

Used in:

animations

retries

background jobs

7️⃣ Time-based Control Flow
setTimeout(cancel, cancelTimeMs);


📌 What you learn

Control behavior based on time

Race between execution and cancellation

This is critical for:

request timeouts

auto-save features

idle detection

8️⃣ Immutability of Returned API
return function cancelFn() { ... }


📌 What you learn

You expose only what is necessary

Internal details (timerId) remain private

This is encapsulation.

🧩 Concept Mapping (Big Picture)
Closures
   ↓
Timers
   ↓
Cancellation
   ↓
Event Loop
   ↓
Encapsulation

🏆 What Interviewers Want You to Say

If asked:

“What concept does this code demonstrate?”

Answer:

This demonstrates closures, cancellable asynchronous execution, event loop scheduling with timers, and higher-order functions.

🌍 Real-World Examples

This exact pattern appears in:

setTimeout wrappers

Debounce implementations

React cleanup functions

API abort mechanisms

Background task managers

🔑 One-Line Summary

This code teaches how to schedule delayed work and safely cancel it using closures and JavaScript’s event loop.
