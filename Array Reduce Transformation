2626. Array Reduce Transformation
Solved
Easy
premium lock icon
Companies
Hint
Given an integer array nums, a reducer function fn, and an initial value init, return the final result obtained by executing the fn function on each element of the array, sequentially, passing in the return value from the calculation on the preceding element.

This result is achieved through the following operations: val = fn(init, nums[0]), val = fn(val, nums[1]), val = fn(val, nums[2]), ... until every element in the array has been processed. The ultimate value of val is then returned.

If the length of the array is 0, the function should return init.

Please solve it without using the built-in Array.reduce method.

 

Example 1:

Input: 
nums = [1,2,3,4]
fn = function sum(accum, curr) { return accum + curr; }
init = 0
Output: 10
Explanation:
initially, the value is init=0.
(0) + nums[0] = 1
(1) + nums[1] = 3
(3) + nums[2] = 6
(6) + nums[3] = 10
The final answer is 10.
Example 2:

Input: 
nums = [1,2,3,4]
fn = function sum(accum, curr) { return accum + curr * curr; }
init = 100
Output: 130
Explanation:
initially, the value is init=100.
(100) + nums[0] * nums[0] = 101
(101) + nums[1] * nums[1] = 105
(105) + nums[2] * nums[2] = 114
(114) + nums[3] * nums[3] = 130
The final answer is 130.
Example 3:

Input: 
nums = []
fn = function sum(accum, curr) { return 0; }
init = 25
Output: 25
Explanation: For empty arrays, the answer is always init.
 

Constraints:

0 <= nums.length <= 1000
0 <= nums[i] <= 1000
0 <= init <= 1000


/**
 * @param {number[]} nums
 * @param {Function} fn
 * @param {number} init
 * @return {number}
 */
var reduce = function(nums, fn, init) {
    let n = nums.length;
    if(n == 0){
        return init;
    }
    for(let i=0;i<n;i++){
        init = fn(init,nums[i]);
    }  
    return init;
};


✅ Code (Reference)
var reduce = function(nums, fn, init) {
    let n = nums.length;
    if (n == 0) {
        return init;
    }
    for (let i = 0; i < n; i++) {
        init = fn(init, nums[i]);
    }
    return init;
};

🧠 Concepts You Learn From This Code
1️⃣ Higher-Order Functions ⭐ (VERY IMPORTANT)
fn(init, nums[i])


fn is a function passed as an argument

Your function calls another function

📌 This is a core JavaScript concept

Interview answer:

A higher-order function is a function that takes another function as an argument or returns one.

2️⃣ Callback Functions

fn is a callback that decides how values are combined.

Examples:

reduce([1,2,3], (a,b) => a + b, 0);
reduce([1,2,3], (a,b) => a * b, 1);


📌 Same reduce, different logic → very powerful.

3️⃣ Accumulator Pattern ⭐⭐⭐
init = fn(init, nums[i]);


This teaches the Accumulator Pattern:

init stores the running result

Each iteration updates it

📌 Used in:

Sum

Product

Max / Min

Object building

Frequency maps

4️⃣ Functional Programming Concept

No mutation of nums

Output depends only on input

Predictable behavior

📌 This is functional programming, used heavily in:

React

Redux

Modern JS

5️⃣ Re-implementing Built-in reduce()

You are recreating:

Array.prototype.reduce()


This means you understand:

How JS array methods work internally

How libraries/frameworks are built

6️⃣ Handling Edge Cases
if (n == 0) return init;


📌 Shows:

Defensive programming

Handling empty arrays correctly

Interviewers LOVE this.

7️⃣ Loop + State Update (DSA Thinking)
for (let i = 0; i < n; i++)


You combine:

Iteration

State update

Conditional logic via callback

This is DSA mindset.

8️⃣ Time & Space Complexity Awareness

Time Complexity: O(n)

Space Complexity: O(1)

📌 Important in interviews.

9️⃣ Abstraction & Reusability

One function → infinite use cases:

sum
product
max
count
map building


📌 This is abstraction, a core CS concept.

10️⃣ Real-World Use Cases

This exact pattern is used to:

Calculate totals in shopping carts

Aggregate API data

Count frequencies

Build objects from arrays

Example:

const freq = nums.reduce((acc, n) => {
    acc[n] = (acc[n] || 0) + 1;
    return acc;
}, {});

🧠 One-Line Summary (MEMORIZE THIS)

This code teaches higher-order functions, callback functions, the accumulator pattern, functional programming, and how JavaScript’s reduce() works internally.

🔥 Interview One-Liner

If asked:

What did you learn from implementing reduce?

Answer:

I learned how accumulation works using callbacks and how complex operations can be abstracted into a single reusable function.
